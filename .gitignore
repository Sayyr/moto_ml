# --- Rust / Cargo ---
/target/
src-tauri/target/
# Cargo.lock EST versionné volontairement (bonne pratique pour un binaire/app,
# contrairement à une lib) : garantit que tout le monde build avec les mêmes
# versions de dépendances.

# --- Node / npm ---
node_modules/
dist/
.vite/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Tauri ---
src-tauri/gen/
src-tauri/target/

# --- Éditeurs / OS ---
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# --- Environnement ---
.env
.env.local